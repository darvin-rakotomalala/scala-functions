package overriding

// Creating a super class
class Shapes {
  // Method defined with parameters
  def Area(l: Double, b: Double, r: Double): Double = {
    0.0 // Utilized for returning double
  }
}
